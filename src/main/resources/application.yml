server:
  port: 8888
spring:
  application:
    name: user-service

eureka:
  instance:
    instance-id: msa-user-service
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
